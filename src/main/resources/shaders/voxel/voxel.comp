#version 430 core
layout(local_size_x = 16, local_size_y = 16) in;
layout(rgba8, binding = 0) uniform writeonly image2D uOutput;
layout(std430, binding = 0) readonly buffer Voxels { uint data[]; };

#include "uniforms.glsl"
#include "main_body.glsl"
